---
import { marked } from 'marked';
import type { GitHubIssueRecord, RepoRecord, ColonyRecord, WorkflowRecord } from '../lib/types';

marked.setOptions({ breaks: true });

export interface IssueWithRepo extends GitHubIssueRecord {
  repo_id: string;
}

interface Props {
  issues: IssueWithRepo[];
  repos: RepoRecord[];
  colonies: ColonyRecord[];
  workflows: WorkflowRecord[];
  showRepoFilter?: boolean;
  repoId?: string;
}

const { issues, repos, colonies, workflows, showRepoFilter = true, repoId } = Astro.props;

function truncateBody(body: string, max: number): string {
  const trimmed = body.trim();
  if (trimmed.length <= max) return trimmed;
  return trimmed.slice(0, max) + '\u2026';
}

// Build a lookup for JS to reference issue data by number
const issueDataMap: Record<number, { title: string; body: string; repoId: string; repoName: string; labels: string[]; state: string }> = {};
for (const issue of issues) {
  const repo = repos.find((r) => r.repo_id === issue.repo_id);
  issueDataMap[issue.number] = {
    title: issue.title,
    body: issue.body || '',
    repoId: issue.repo_id,
    repoName: repo?.full_name || '',
    labels: issue.labels,
    state: issue.state,
  };
}
---

<script id="issue-data-json" type="application/json" set:html={JSON.stringify(issueDataMap)} />

{showRepoFilter && (
  <div class="repo-filter">
    <label class="form-label" for="repo-select">Repository</label>
    <select class="form-select" id="repo-select">
      <option value="all">All repos</option>
      {repos.map((r) => (
        <option value={r.repo_id}>{r.full_name}</option>
      ))}
    </select>
  </div>
)}

<div class="data-panel" data-repo-id={repoId}>
  <div class="data-panel-header">
    <span class="data-panel-title">Open Issues</span>
    <span class="content-count" id="issue-count">{issues.length}</span>
    {repoId && (
      <button class="btn btn--sm" id="refresh-issues-btn" style="margin-left: auto;">Refresh</button>
    )}
  </div>
  <div class="data-panel-body">
    {issues.length === 0 ? (
      <div class="empty-state">
        <div class="empty-state-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
        </div>
        <div class="empty-state-title">No open issues</div>
        <p class="empty-state-desc">All GitHub issues are resolved.</p>
      </div>
    ) : (
      <div class="issue-list" id="issue-list">
        {issues.map((issue) => {
          const repo = repos.find((r) => r.repo_id === issue.repo_id);
          const body = (issue.body || '').trim();
          const isLong = body.length > 180;
          const snippetMd = isLong ? truncateBody(body, 180) : body;
          return (
            <div class="issue-card" data-issue-number={issue.number} data-repo-id={issue.repo_id}>
              <div class="issue-card-header">
                <div class="issue-card-title">
                  <span class="issue-number">#{issue.number}</span>
                  {issue.title}
                </div>
                <div class="issue-card-actions">
                  {issue.already_queued ? (
                    <span class="badge badge--muted">Queued</span>
                  ) : (
                    <button
                      class="btn btn--primary btn--sm launch-mission-btn"
                      data-issue-number={issue.number}
                    >
                      Create Mission
                    </button>
                  )}
                </div>
              </div>
              {snippetMd && (
                <div class="issue-card-body issue-prose issue-card-body--clamp" set:html={marked.parse(snippetMd)} />
              )}
              {isLong && (
                <button class="issue-expand-btn" data-issue-number={issue.number}>&hellip;</button>
              )}
              <div class="issue-card-footer">
                {repo && (
                  <span>{repo.full_name}</span>
                )}
                {issue.labels.map((label) => (
                  <span class="issue-label">{label}</span>
                ))}
                <span class={`issue-state issue-state--${issue.state}`}>{issue.state}</span>
              </div>
            </div>
          );
        })}
      </div>
    )}
  </div>
</div>

<!-- Mission Launch Modal -->
<dialog class="modal" id="launch-modal">
  <div class="modal-content">
    <div class="modal-header">
      <span class="modal-title">Create Mission</span>
      <button class="modal-close" id="launch-modal-close">&times;</button>
    </div>
    <div class="modal-body">
      <div class="launch-issue-ref" id="launch-issue-ref"></div>
      <div class="launch-repo-ref" id="launch-repo-ref"></div>

      <div class="form-group">
        <label class="form-label" for="launch-colony">Colony</label>
        <select class="form-select" id="launch-colony">
          {colonies.map((c) => (
            <option value={c.colony_id}>{c.name}</option>
          ))}
          <option value="__new__">Create new&hellip;</option>
        </select>
      </div>

      <div class="form-group" id="launch-new-colony-group" hidden>
        <label class="form-label" for="launch-new-colony-name">New colony name</label>
        <input type="text" class="form-input" id="launch-new-colony-name" placeholder="my-colony" />
      </div>

      <div class="form-group">
        <label class="form-label" for="launch-workflow">Workflow (optional)</label>
        <select class="form-select" id="launch-workflow">
          <option value="">None</option>
          {workflows.map((wf) => (
            <option value={wf.name}>{wf.name}</option>
          ))}
        </select>
      </div>
    </div>
    <div class="modal-footer">
      <button class="btn" id="launch-cancel">Cancel</button>
      <button class="btn btn--primary" id="launch-execute">Create</button>
    </div>
  </div>
</dialog>

<!-- Issue Detail Modal -->
<dialog class="modal modal--wide" id="issue-detail-modal">
  <div class="modal-content">
    <div class="modal-header">
      <span class="modal-title" id="issue-detail-title"></span>
      <button class="modal-close" id="issue-detail-close">&times;</button>
    </div>
    <div class="modal-body">
      <div class="issue-detail-meta" id="issue-detail-meta"></div>
      <div class="issue-detail-body issue-prose" id="issue-detail-body"></div>
    </div>
  </div>
</dialog>

<script>
  import { marked } from 'marked';
  marked.setOptions({ breaks: true });

  // Load issue data from JSON instead of data attributes
  const issueDataEl = document.getElementById('issue-data-json');
  const issueDataMap: Record<number, { title: string; body: string; repoId: string; repoName: string; labels: string[]; state: string }> =
    issueDataEl ? JSON.parse(issueDataEl.textContent || '{}') : {};

  const modal = document.getElementById('launch-modal') as HTMLDialogElement;
  const issueRef = document.getElementById('launch-issue-ref')!;
  const repoRef = document.getElementById('launch-repo-ref')!;
  const closeBtn = document.getElementById('launch-modal-close')!;
  const cancelBtn = document.getElementById('launch-cancel')!;
  const executeBtn = document.getElementById('launch-execute') as HTMLButtonElement;
  const repoSelect = document.getElementById('repo-select') as HTMLSelectElement | null;
  const issueList = document.getElementById('issue-list')!;
  const issueCount = document.getElementById('issue-count')!;
  const refreshBtn = document.getElementById('refresh-issues-btn') as HTMLButtonElement | null;
  const dataPanel = document.querySelector<HTMLElement>('.data-panel[data-repo-id]');
  const colonySelect = document.getElementById('launch-colony') as HTMLSelectElement;
  const newColonyGroup = document.getElementById('launch-new-colony-group')!;
  const newColonyInput = document.getElementById('launch-new-colony-name') as HTMLInputElement;
  const workflowSelect = document.getElementById('launch-workflow') as HTMLSelectElement;

  const detailModal = document.getElementById('issue-detail-modal') as HTMLDialogElement;
  const detailTitle = document.getElementById('issue-detail-title')!;
  const detailMeta = document.getElementById('issue-detail-meta')!;
  const detailBody = document.getElementById('issue-detail-body')!;
  const detailClose = document.getElementById('issue-detail-close')!;

  detailClose.addEventListener('click', () => detailModal.close());
  detailModal.addEventListener('click', (e) => {
    if (e.target === detailModal) detailModal.close();
  });

  function bindExpandButtons() {
    document.querySelectorAll<HTMLButtonElement>('.issue-expand-btn').forEach((btn) => {
      btn.addEventListener('click', () => {
        const num = Number(btn.dataset.issueNumber);
        const data = issueDataMap[num];
        if (!data) return;

        detailTitle.textContent = `#${num} — ${data.title}`;
        detailMeta.innerHTML = [
          data.repoName ? `<span>${data.repoName}</span>` : '',
          ...data.labels.map((l) => `<span class="issue-label">${l}</span>`),
          data.state ? `<span class="issue-state issue-state--${data.state}">${data.state}</span>` : '',
        ].filter(Boolean).join(' ');
        detailBody.innerHTML = marked.parse(data.body) as string;
        detailModal.showModal();
      });
    });
  }
  bindExpandButtons();

  let currentIssueNumber: number | null = null;

  colonySelect.addEventListener('change', () => {
    newColonyGroup.hidden = colonySelect.value !== '__new__';
  });

  // Repo filter
  repoSelect?.addEventListener('change', () => {
    const val = repoSelect.value;
    let visible = 0;
    issueList.querySelectorAll<HTMLElement>('.issue-card').forEach((card) => {
      const show = val === 'all' || card.dataset.repoId === val;
      card.style.display = show ? '' : 'none';
      if (show) visible++;
    });
    issueCount.textContent = String(visible);
  });

  // Refresh issues
  refreshBtn?.addEventListener('click', async () => {
    const repoId = dataPanel?.dataset.repoId;
    if (!repoId) return;

    refreshBtn.textContent = 'Refreshing...';
    refreshBtn.disabled = true;

    try {
      const res = await fetch(`/api/repos/${repoId}/issues`);
      if (!res.ok) throw new Error(`HTTP ${res.status}`);
      const issues: Array<{
        number: number;
        title: string;
        body: string;
        labels: string[];
        state: string;
        already_queued: boolean;
      }> = await res.json();

      // Update the data map with refreshed issues
      for (const issue of issues) {
        issueDataMap[issue.number] = {
          title: issue.title,
          body: issue.body || '',
          repoId,
          repoName: '',
          labels: issue.labels,
          state: issue.state,
        };
      }

      issueList.innerHTML = issues.map((issue) => {
        const body = (issue.body || '').trim();
        const isLong = body.length > 180;
        const snippetMd = isLong ? body.slice(0, 180) + '\u2026' : body;
        return `
        <div class="issue-card" data-issue-number="${issue.number}" data-repo-id="${repoId}">
          <div class="issue-card-header">
            <div class="issue-card-title">
              <span class="issue-number">#${issue.number}</span>
              ${issue.title}
            </div>
            <div class="issue-card-actions">
            ${issue.already_queued
              ? '<span class="badge badge--muted">Queued</span>'
              : `<button class="btn btn--primary btn--sm launch-mission-btn"
                  data-issue-number="${issue.number}"
                >Create Mission</button>`}
            </div>
          </div>
          ${snippetMd ? `<div class="issue-card-body issue-prose issue-card-body--clamp">${marked.parse(snippetMd)}</div>` : ''}
          ${isLong ? `<button class="issue-expand-btn" data-issue-number="${issue.number}">&hellip;</button>` : ''}
          <div class="issue-card-footer">
            ${issue.labels.map((l) => `<span class="issue-label">${l}</span>`).join('')}
            <span class="issue-state issue-state--${issue.state}">${issue.state}</span>
          </div>
        </div>
      `}).join('');

      issueCount.textContent = String(issues.length);
      bindLaunchButtons();
      bindExpandButtons();
    } catch (e) {
      console.error('Failed to refresh issues:', e);
    } finally {
      refreshBtn.textContent = 'Refresh';
      refreshBtn.disabled = false;
    }
  });

  // Launch modal
  function resetModal() {
    colonySelect.selectedIndex = 0;
    newColonyGroup.hidden = true;
    newColonyInput.value = '';
    workflowSelect.selectedIndex = 0;
    executeBtn.textContent = 'Create';
    executeBtn.disabled = false;
    currentIssueNumber = null;
  }

  function bindLaunchButtons() {
    document.querySelectorAll<HTMLButtonElement>('.launch-mission-btn').forEach((btn) => {
      btn.addEventListener('click', () => {
        resetModal();
        const num = Number(btn.dataset.issueNumber);
        const data = issueDataMap[num];
        currentIssueNumber = num;
        issueRef.textContent = `#${num} — ${data?.title || ''}`;
        repoRef.textContent = data?.repoName || '';
        modal.showModal();
      });
    });
  }
  bindLaunchButtons();

  closeBtn.addEventListener('click', () => { modal.close(); resetModal(); });
  cancelBtn.addEventListener('click', () => { modal.close(); resetModal(); });
  modal.addEventListener('click', (e) => {
    if (e.target === modal) { modal.close(); resetModal(); }
  });

  executeBtn.addEventListener('click', async () => {
    if (!currentIssueNumber) return;

    executeBtn.textContent = 'Creating...';
    executeBtn.disabled = true;

    try {
      let colonyId = colonySelect.value;

      // Create colony if needed
      if (colonyId === '__new__') {
        const name = newColonyInput.value.trim();
        if (!name) {
          alert('Colony name is required.');
          executeBtn.textContent = 'Create';
          executeBtn.disabled = false;
          return;
        }
        const colonyRes = await fetch('/api/colonies', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ name }),
        });
        if (!colonyRes.ok) {
          const err = await colonyRes.json().catch(() => ({ error: 'Unknown error' }));
          throw new Error(err.error || `Failed to create colony: ${colonyRes.status}`);
        }
        const colony = await colonyRes.json();
        colonyId = colony.colony_id;
      }

      // Queue the issue as a mission
      const body: Record<string, unknown> = { issue_number: currentIssueNumber };
      const workflow = workflowSelect.value;
      if (workflow) body.workflow = workflow;

      const res = await fetch(`/api/colonies/${colonyId}/queue`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(body),
      });

      if (!res.ok) {
        const err = await res.json().catch(() => ({ error: 'Unknown error' }));
        throw new Error(err.error || `Failed to queue issue: ${res.status}`);
      }

      modal.close();
      resetModal();
      window.location.reload();
    } catch (e: unknown) {
      executeBtn.textContent = 'Create';
      executeBtn.disabled = false;
      alert(`Error: ${e instanceof Error ? e.message : e}`);
    }
  });
</script>

<style>
  .issue-card-actions {
    display: flex;
    align-items: center;
    gap: 6px;
    flex-shrink: 0;
  }

  .issue-card-body--clamp {
    max-height: 4.8em;
    overflow: hidden;
    position: relative;
  }

  .issue-expand-btn {
    background: none;
    border: none;
    color: var(--primary, #2563eb);
    cursor: pointer;
    font-size: 13px;
    font-weight: 600;
    padding: 2px 6px;
    border-radius: 4px;
    margin-top: 4px;
    letter-spacing: 0.1em;
  }

  .issue-expand-btn:hover {
    background: var(--primary-soft, #eff6ff);
  }

  .issue-detail-meta {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
    margin-bottom: 12px;
    font-size: 13px;
    color: var(--muted);
  }

  .issue-detail-body :global(p) { margin: 0 0 8px; }
  .issue-detail-body :global(p:last-child) { margin-bottom: 0; }
  .issue-detail-body :global(h1),
  .issue-detail-body :global(h2),
  .issue-detail-body :global(h3) {
    font-weight: 700;
    margin: 16px 0 6px;
  }
  .issue-detail-body :global(h1:first-child),
  .issue-detail-body :global(h2:first-child),
  .issue-detail-body :global(h3:first-child) { margin-top: 0; }
  .issue-detail-body :global(h1) { font-size: 17px; }
  .issue-detail-body :global(h2) { font-size: 15px; }
  .issue-detail-body :global(h3) { font-size: 14px; }
  .issue-detail-body :global(ul),
  .issue-detail-body :global(ol) { margin: 4px 0 8px; padding-left: 20px; }
  .issue-detail-body :global(li) { margin-bottom: 2px; }
  .issue-detail-body :global(code) {
    background: var(--neutral-bg, #f3f4f6);
    padding: 1px 5px;
    border-radius: 3px;
    font-size: 12px;
    font-family: var(--font-mono, monospace);
  }
  .issue-detail-body :global(pre) {
    background: var(--neutral-bg, #f3f4f6);
    padding: 10px 12px;
    border-radius: 6px;
    overflow-x: auto;
    margin: 6px 0 8px;
  }
  .issue-detail-body :global(pre code) { background: none; padding: 0; }
  .issue-detail-body :global(blockquote) {
    border-left: 3px solid var(--line, #e5e7eb);
    margin: 6px 0;
    padding: 2px 12px;
    color: var(--muted, #6b7280);
  }
  .issue-detail-body :global(a) { color: var(--primary, #2563eb); text-decoration: none; }
  .issue-detail-body :global(a:hover) { text-decoration: underline; }

  #issue-detail-modal {
    width: 800px;
    max-width: 90vw;
  }
</style>
