---
import type { MissionRecord, TaskRecord, ColonyRecord } from '../lib/types';
import { timeAgo } from '../lib/format';

interface Props {
  missions: MissionRecord[];
  tasks: TaskRecord[];
  colonies: ColonyRecord[];
}

const { missions, tasks, colonies } = Astro.props;

function taskIcon(status: string): string {
  switch (status) {
    case 'completed': return '&#10003;';
    case 'running': return '&#9654;';
    case 'failed': return '&#10007;';
    case 'blocked': return '&#9632;';
    case 'assigned': return '&#9654;';
    default: return '&#8226;';
  }
}

function truncate(text: string, max: number): string {
  if (text.length <= max) return text;
  return text.slice(0, max) + '\u2026';
}

function colonyName(colonyId: string): string {
  const colony = colonies.find((c) => c.colony_id === colonyId);
  return colony?.name || colonyId.slice(0, 8);
}

function tasksForMission(missionId: string): TaskRecord[] {
  return tasks
    .filter((t) => t.mission_id === missionId)
    .sort((a, b) => a.created_at_ms - b.created_at_ms);
}
---

<div class="data-panel">
  <div class="data-panel-header">
    <span class="data-panel-title">Missions</span>
    <span class="content-count">{missions.length}</span>
  </div>
  <div class="data-panel-body">
    {missions.length === 0 ? (
      <div class="empty-state">
        <div class="empty-state-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 5v14M5 12h14"/></svg>
        </div>
        <div class="empty-state-title">No missions yet</div>
        <p class="empty-state-desc">Create one to get started.</p>
      </div>
    ) : (
      <div class="mission-list">
        {missions.map((mission) => {
          const mTasks = tasksForMission(mission.mission_id);
          return (
            <div class="mission-card" data-mission-id={mission.mission_id}>
              <div class="mission-card-header">
                <div class="mission-card-title">
                  <span class={`badge badge--${mission.status}`}>{mission.status}</span>
                  <span class="mission-title-text">{truncate(mission.prompt, 80)}</span>
                </div>
                <div class="mission-card-meta">
                  <span>{colonyName(mission.colony_id)}</span>
                  {mission.workflow_name && (
                    <code class="branch-name">{mission.workflow_name}</code>
                  )}
                  {mission.github_issue_number && (
                    <span class="issue-number">#{mission.github_issue_number}</span>
                  )}
                  {mission.github_pr_number && (
                    <span class="pr-ref">PR #{mission.github_pr_number}</span>
                  )}
                  <span class="mission-time">{timeAgo(mission.created_at_ms)}</span>
                </div>
              </div>

              {mTasks.length > 0 && (
                <div class="pipeline">
                  {mTasks.map((task, i) => (
                    <>
                      <div class={`pipeline-step pipeline-step--${task.status}`}>
                        <div class="pipeline-step-icon" set:html={taskIcon(task.status)} />
                        <div class="pipeline-step-info">
                          <div class="pipeline-step-name">{task.title}</div>
                        </div>
                      </div>
                      {i < mTasks.length - 1 && (
                        <div class={`pipeline-connector ${task.status === 'completed' ? 'pipeline-connector--done' : ''}`} />
                      )}
                    </>
                  ))}
                </div>
              )}
            </div>
          );
        })}
      </div>
    )}
  </div>
</div>
